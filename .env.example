# Vite는 VITE_ 접두사 변수만 클라이언트 번들에 노출한다.
# 실제 값은 .env.local(gitignore) 또는 배포 환경변수로 주입하고, 이 파일은 키 목록만 유지한다.

# API 기본 URL. 1주차에는 MSW mock을 사용하므로 비워둬도 됨.
VITE_API_BASE_URL=

# MSW(Mock Service Worker) on/off. 1주차 true, BE dev API 준비 후 false로 전환.
VITE_ENABLE_MSW=true
